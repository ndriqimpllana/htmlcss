const likeButton = document.getElementById('likeButton');

subscribeButton.addEventListener('click', () => {
  subscribeButton.classList.add('pop', 'flip'); // Add animation classes
  // You can add additional logic here, like updating a counter or sending data
});
const subscribeButton = document.getElementById('subscribeButton');

subscribeButton.addEventListener('click', () =>    {
  subscribeButton.classList.add('pop', 'flip');
}